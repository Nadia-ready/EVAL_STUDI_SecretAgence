{% extends 'admin/base.html.twig' %}

{% block main %}

    {{ form(missionForm) }}

    <button class="btn btn-outline-dark"
            type="button">

        <a href="{{ path('admin_missions_list') }}">Retourner à la liste des missions</a>
    </button>


{% endblock main %}