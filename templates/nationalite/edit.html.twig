{% extends 'base.html.twig' %}

{% block title %}Créer une nationalité{% endblock %}

{% block main %}
    <h1>Créer une nationalité</h1>

    {{ include('nationalite/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('nationalite_index') }}" class="btn btn-primary">Retour à la liste</a>

    {{ include('nationalite/_delete_form.html.twig') }}
{% endblock %}
