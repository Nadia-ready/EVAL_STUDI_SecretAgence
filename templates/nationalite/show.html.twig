{% extends 'admin/base.html.twig' %}

{% block title %}Nationalite{% endblock %}

{% block main %}
    <h1>Nationalite</h1>

    <table class="table table-dark table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nationalite.id }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ nationalite.pays }}</td>
            </tr>
            <tr>
                <th>Nationalite</th>
                <td>{{ nationalite.nationalite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('nationalite_index') }}" class="btn btn-primary">Retour à la liste</a>

    <a href="{{ path('nationalite_edit', {'id': nationalite.id}) }}" class="btn btn-primary">Modifier</a>

    {{ include('nationalite/_delete_form.html.twig') }}
{% endblock %}
