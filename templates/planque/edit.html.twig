{% extends 'admin/base.html.twig' %}

{% block title %}Modifier une planque{% endblock %}

{% block main %}
    <h1>Modifier une planque</h1>

    {{ include('planque/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('planque_index') }}" class="btn btn-primary">Retour à la liste</a>

    {{ include('planque/_delete_form.html.twig') }}
{% endblock %}
