{% extends 'admin/base.html.twig' %}

{% block title %}Planque{% endblock %}

{% block main %}
    <h1>Planque</h1>

    <table class="table table-dark table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ planque.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ planque.code }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ planque.adresse }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ planque.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('planque_index') }}" class="btn btn-primary">Retour à la liste</a>

    <a href="{{ path('planque_edit', {'id': planque.id}) }}" class="btn btn-primary">Modifier</a>

    {{ include('planque/_delete_form.html.twig') }}
{% endblock %}
