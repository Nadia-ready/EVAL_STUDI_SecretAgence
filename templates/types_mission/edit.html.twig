{% extends 'admin/base.html.twig' %}

{% block title %}Créer un nouveau type de mission{% endblock %}

{% block main %}
    <h1>Créer un nouveau type de mission</h1>

    {{ include('types_mission/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('types_mission_index') }}" class="btn btn-primary">Retour à la liste</a>

    {{ include('types_mission/_delete_form.html.twig') }}
{% endblock %}
