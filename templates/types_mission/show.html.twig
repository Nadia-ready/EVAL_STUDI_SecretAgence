{% extends 'base.html.twig' %}

{% block title %}TypesMission{% endblock %}

{% block body %}
    <h1>TypesMission</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ types_mission.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ types_mission.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('types_mission_index') }}">back to list</a>

    <a href="{{ path('types_mission_edit', {'id': types_mission.id}) }}">edit</a>

    {{ include('types_mission/_delete_form.html.twig') }}
{% endblock %}
